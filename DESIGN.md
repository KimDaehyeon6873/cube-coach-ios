# Design

## Source of truth

- Status: Active
- Last refreshed: 2026-07-31
- Primary product surfaces: 오늘, 학습(모든 단서 공개·따라 하기), 연습(스크램블·검산 전개도·타이머), 기록. 물리 트레이너는 독립 탭이 아니라 `복습` 흐름이다.
- Evidence reviewed:
  - `README.md`
  - `Package.swift`
  - `CubeCoachApp/App/**`
  - `CubeCoachApp/Features/**`
  - `CubeCoachApp/DesignSystem/CoachTheme.swift`
  - `CubeCoachApp/Infrastructure/Camera/**`
  - `Sources/CubeCoachCore/**`
  - `docs/PRODUCT_SPEC.md`
  - `docs/RESEARCH.md`
  - `docs/ARCHITECTURE.md`
  - `docs/LEARNING_EXPERIENCE.md`
- Observed facts:
  - 앱은 한국어를 기본 언어로 하고 SwiftUI 탭 구조를 사용한다.
  - 현재 기본 탭은 오늘, 학습, 연습, 기록 네 개다. 내부 `TrainerView`는 오늘 또는 학습 상세에서 여는 사용자-facing `복습` 흐름이며 독립 탭이 아니다.
  - 학습은 설명·인식 단서·공식·회전별 전후 상태를 모두 공개하고 따라 돌리는 열람 경험이다. 학습 화면을 본 것만으로 복습·숙달 기록을 만들지 않는다.
  - 복습은 단서를 가린 시작 상태에서 실제 큐브로 수행하고 기대 상태와 비교한 뒤에만 시도 기록을 만든다. `H0`–`H5`, `externallyPrepared`, `guidedAcquisition`은 내부 데이터·측정 용어이며 사용자 UI에는 노출하지 않는다.
  - 복습 내부 모델은 준비 방식을 `externallyPrepared`와 `guidedAcquisition`으로 구분한다. 정답 역순의 설정 동작을 연 시도는 지원된 습득으로 기록하고 독립 H0로 세지 않는다.
  - 보라색 강조색, 초록색 성공색, 주황색 경고색과 둥근 카드가 초기 테마로 존재한다.
  - 카메라 계층은 안내식 두 포즈에서 54칸 후보와 수동 교정·상태 유효성 검사를 제공하지만 실기기 정확도 검증과 범용 사진 인식은 완료되지 않았다.
  - 스캔 진입의 `scanRecommendation`은 진단 단계에 대응하는 대표 내장 연습이다. 추천 전개도는 촬영한 54칸 상태 자체가 아니며 사후 `validatedScan`도 아직 연결되지 않았다.
  - 현재 스크램블은 TNoodle-WCA 1.2.3으로 사전 생성한 32,768개 번들 카탈로그이며 공식 대회용이 아니다.
  - 연습 탭 자체가 별도 화면 깊이 없는 타이머 루트다. `ScrollView` 없이 인라인 내비게이션, 모드 선택, TNoodle 스크램블, 축약 전개도와 U/F 검산 기준, 타이머·주요 조작을 한 화면에 맞춘다. `오늘`의 CTA도 push가 아니라 이 탭으로 전환한다.
  - 스캔은 상단의 보조 진입점이며 타이머 화면에는 `방금 기록`과 페널티 수정만 남긴다. PB·평균·추세 등 통계의 단일 사용자-facing 소스는 기록 탭이다.
  - 기본·다크·틴트용 1024px 앱 아이콘 자산이 포함되어 있다.
- Design assumptions:
  - 첫 제품 범위는 3×3×3과 iPhone 세로 화면이다.
  - 사용자는 실제 큐브를 한 손 또는 두 손으로 들고 다른 손으로 앱을 조작할 수 있다.
  - 기존 평문·자기평가형 회상 카드는 일반 기억 연구를 큐브 시각·운동 과제에 과도하게 확장했다. 새 계약은 2D 상태, 방향, 실제 동작, 기대 상태 비교, 지연 재시도를 사용한다.
  - 이론·전체 예시와 역공식 설정은 습득 지원이며 숙달로 세지 않는다. 사용자가 앱 밖에서 정확한 시작 상태를 이미 만들었다고 명시한 경우에만 독립 H0 후보가 된다.

## Brand

- Personality: 차분한 코치, 정확한 기록 도구, 실수를 학습 기회로 바꾸는 동료
- Identity:
  - 앱 아이콘은 흰색 U를 위로 둔 단순한 3면 큐브와, 한쪽 3칸 레이어의 강조선으로 `방향을 고정하고 한 동작씩 연습한다`는 의미를 전달한다.
  - 앱 아이콘은 `Tools/generate_app_icons.swift`에서 기본·다크·틴트 1024px 자산을 재현 가능하게 생성한다.
  - 브랜드 보라는 내비게이션·주요 행동에만 쓰고, 큐브 스티커 색은 상태 설명에만 쓴다.
- Trust signals:
  - 공식과 스크램블의 출처·버전 표시
  - 인식 신뢰도와 불확실한 스티커를 숨기지 않는 UI
  - “연습용”, “WCA 규정 참고”, “공식 대회용 아님”을 구분하는 라벨
  - 카메라 데이터의 기기 내 처리와 저장 여부를 촬영 전에 설명
- Avoid:
  - “사진 한 번이면 무조건 해결”, “세계 최단 보장” 같은 과장
  - 사용자의 실패를 벌주는 언어와 과도한 연속 학습 유도
  - 무지개색 장식, 네온 게임 UI, 큐브 여섯 색을 모두 강조색으로 쓰는 시각적 소음
  - WCA 또는 특정 큐브 브랜드의 공식 앱처럼 보이는 표현
  - 자동 해결을 연상시키는 순환 화살표·완료 체크·마법봉, 광택·그라디언트·과도한 그림자
  - U/F 기준 방향과 충돌하는 임의의 큐브 방향·색 배치

## Product goals

- Goals:
  - 시작 상태와 방향을 보고 실제 큐브에서 공식 동작을 재현하게 한다.
  - 지연 시도에서 사용한 도움 수준과 물리 결과 증거를 구분해 기록한다.
  - 초급 해법에서 2-Look CFOP, Full CFOP으로 무리 없이 이동하게 한다.
  - 저마찰 타이머와 읽기 쉬운 통계로 학습과 실전 기록을 연결한다.
  - 여러 면이 보이는 촬영으로 수동 6면 입력 부담을 줄인다.
- Non-goals:
  - 사용자를 대신해 매번 전체 해결 순서를 낭독하는 범용 자동 해결기
  - 공식 WCA 대회 타이밍 또는 스크램블 시스템 대체
  - 첫 출시에서 모든 퍼즐 종목과 모든 해법 지원
  - 근거 없이 “초급/중급/고급”을 보편적 등급처럼 단정
  - 평문 답안, 공식 문자열 입력, 기억 난이도 자기평가를 큐브 실행의 대리 지표로 사용
  - 불완전한 커리큘럼을 완료·숙달로 표시
- Success signals:
  - 현재는 `externallyPrepared`·H0·미교정·playback 미사용 조건의 독립 시도와 `guidedAcquisition` 또는 H1–H5 등 지원 시도의 수동 일치 보고를 구분한다. 후속 `validatedScan`이 실기기 게이트를 통과하면 스캔 검증 일치율도 별도로 관찰
  - 같은 케이스의 힌트 단계가 시간에 따라 낮아짐
  - 학습 케이스가 섞인 자유 솔빙에서 인식·실행 성공
  - 스캔 완료율과 수동 수정량 개선
  - 사용자가 자동 안내보다 실제 큐브의 독립 실행을 더 자주 선택

## Personas and jobs

- Primary personas:
  - 처음 맞추는 법은 배웠지만 공식을 자주 잊는 입문자
  - 초급 해법에서 2-Look CFOP/F2L로 전환하려는 성장형 큐버
  - OLL·PLL 등 많은 케이스를 체계적으로 복습하려는 중급 큐버
- User jobs:
  - “지금 봐야 할 케이스를 짧게 복습하고 싶다.”
  - “공식을 바로 보지 않고 내가 기억하는지 시험하고 싶다.”
  - “실제 큐브 상태가 어떤 케이스인지 확인하고 싶다.”
  - “연습 기록과 약한 케이스를 함께 보고 싶다.”
- Key contexts of use:
  - 책상 위에서 실제 큐브와 함께 5–15분 복습
  - 대회 전 WCA 규정과 유사한 검사 시간을 켠 타이머 연습
  - 이동 중 큐브 없이 보는 내용은 이론·설정 복습으로 제한하고 물리 실행 성공으로 세지 않음
  - 조명과 배경이 다양한 실내에서 한 손 촬영

## Information architecture

- Primary navigation:
  - 오늘
  - 학습
  - 연습
  - 기록
- Core routes/screens:
  - 오늘의 복습 → 시작 상태 준비 → 단서를 가리고 실제 큐브 실행 → 기대 상태 수동 비교 → 결과
  - 학습 로드맵 → 설명·인식 단서·공식 전체 공개 → 회전별 전후 상태 → 실제 큐브 따라 하기. 열람만으로 기록하지 않음
  - 오늘의 `연습 시작` 또는 연습 탭 → 탭 전환만으로 한 화면의 TNoodle 연습 스크램블과 축약 검산 전개도 → 같은 주요 조작 영역의 선택적 15초 인스펙션 → 타이머 → 방금 기록·페널티
  - 연습의 보조 진입 → 내 큐브 확인 → 포즈 A/B → 상태 확인 → 단계 진단 → 대표 복습 추천
  - 기록 → 현재 수동 비교·약한 케이스 → 재학습. 후속 validated scan 통계는 별도 증거로 추가
- Content hierarchy:
  1. 지금 할 한 가지 행동
  2. 시작 2D 상태와 기준 방향
  3. `방향 보기`, `첫 회전 보기`, `전체 회전 보기`처럼 사용자가 이해할 수 있는 단계별 도움
  4. 실제 큐브와 비교할 기대 상태
  5. 출처, 대체 공식, 고급 분석

## Timer and statistics benchmark

- 사용자 지정 1순위 벤치마크는 [CubeTime](https://apps.apple.com/kr/app/cubetime/id1600392245)이다.
- 참고할 이유:
  - 타이머의 시작·정지와 다음 스크램블 진입 사이 마찰이 낮다.
  - 작은 iPhone 화면에서도 세션과 핵심 통계를 빠르게 읽을 수 있다.
  - current/best ao5·ao12·ao100, mean·median·best single, 추세·분포 그래프를 단계적으로 탐색할 수 있다.
  - session, multiphase, comp sim, 일괄 기록 편집·내보내기, 설정 확장 사례를 제공한다.
- CubeCoach 적용 원칙:
  - 사용자-facing 이름은 탭·내비게이션 제목 모두 `연습`으로 통일한다. `스크램블-타이머`와 `스피드 타이머`를 혼용한 이전 구조는 정당화되지 않는다.
  - 타이머 화면은 탐색 깊이와 세로 스크롤 없이 인라인 제목, 모드 선택, 스크램블, 축약 검산 전개도, 시간, 하나의 주요 조작, 방금 기록·페널티를 한 화면에 배치한다.
  - WCA 방식 연습도 별도 화면이나 보조 버튼 열을 만들지 않고 같은 주요 조작 위치에서 인스펙션 시작과 타이머 준비를 이어 간다.
  - 정지 상태는 최신 시간과 페널티만 보여 주고 다음 스크램블을 한 번만 게시한다. 평균·최근 12개 목록은 기록 탭과 중복되므로 제거한다.
  - 기록 화면은 `PB → 현재·최고 평균 → 추세 → 완주율·일관성 → 학습 지표` 순으로 깊이를 늘린다.
  - 학습 지표에는 H0–H5 최고 도움과 수동 일치 보고를 기록한다. 지연 구간과 스캔 검증은 후속 필드로 분리해 도입한다.
  - CubeTime의 외형, 카드 구성, 아이콘, 그래프 스타일을 복제하지 않는다. 검증된 정보 구조와 저마찰 원칙만 참고한다.
- 구현 경계:
  - 현재 타이머 UI는 `방금 기록`과 페널티 수정만 노출한다. 기록 UI가 PB·session mean·완주율·최근 일관성·current/best ao5·ao12·최근 10/25/50회 추세·기본 학습 통계를 소유한다.
  - ao100, median, 분포, 일괄 편집·내보내기는 목표 상태이며 현재 완료된 기능으로 표현하지 않는다.
  - TNoodle 1.2.3 사전 생성 출처를 정확히 밝히되 CubeTime과의 동등성이나 공식 대회 용도를 주장하지 않는다.

## Design principles

- Principle 1 — 물리 회상 우선: 큐브 상태에서 방향을 고정하고 실제 동작을 수행한 뒤 기대 상태를 연다.
- Principle 2 — 한 화면, 한 과제: 촬영·회상·실행·평가를 같은 순간에 경쟁시키지 않는다.
- Principle 3 — 불확실성 공개: 인식 실패를 숨기거나 임의로 확정하지 않고 수정 가능한 후보를 보여 준다.
- Principle 4 — 실제 큐브가 주인공: MVP의 2D net·stepper는 방향과 상태 변화를 설명하되 손의 연습을 대체하지 않는다.
- Principle 5 — 규정과 연습의 구분: WCA 규정을 참고한 기능과 공식 대회 기능을 명확히 나눈다.
- Principle 6 — 증거 수준 분리: 현재 수동 일치 보고를 자동 검증으로 부르지 않는다. 후속 유효 상태 스캔을 도입해도 별도 증거로 저장하고 `검증됨`은 검증 게이트 통과 뒤에만 쓴다.
- Principle 7 — 준비 지원 공개: 정답 역순의 설정 동작은 답을 누설하지 않는 테스트 스크램블이 아니라 `guidedAcquisition`이다. 이를 본 시도는 독립 H0로 표시하지 않는다.
- Principle 8 — 사용자 용어와 측정 용어 분리: UI는 `학습`, `복습`, `도움 보기`를 사용하고 `Trainer`, `H0`–`H5`, `guidedAcquisition`은 구현·분석 계약에만 둔다.
- Tradeoffs:
  - 평문으로 공식을 쓰는 편의보다 실제 큐브 실행과 상태 비교를 우선한다.
  - 완전 자동 스캔보다 확인·수정 가능한 정확성을 우선한다.
  - 기능 밀도보다 한 손 조작, 큰 타깃, 읽기 쉬운 표기를 우선한다.
  - 최단 수보다 사용자가 선택한 해법의 인식과 재현 가능성을 우선한다.

## Visual language

- Color:
  - 페이지는 시스템 grouped 배경, 카드는 secondary grouped 배경을 사용해 라이트·다크 모드의 계층을 유지한다.
  - 주요 강조색은 `coachAccent` 인디고를 사용한다.
  - 성공은 초록, 주의·검사 시간 임박은 주황, 오류는 시스템 빨강을 사용한다.
  - 큐브 스티커 여섯 색은 상태 표현에만 예약하고 버튼 의미 색으로 재사용하지 않는다.
- Typography:
  - Dynamic Type을 지원하는 시스템 글꼴을 사용한다.
  - 공식은 모노스페이스 또는 rounded 디자인과 tabular 숫자를 사용하되 본문 접근성을 해치지 않는다.
  - 프라임(`′`)과 숫자(`2`)가 작은 크기에서도 구분되게 한다.
- Spacing/layout rhythm:
  - 4pt 기반 간격, 주요 화면 16pt 여백, 섹션 간 20–24pt
  - 한 카드 안에는 인식·힌트·공식 중 현재 필요한 층만 표시
- Shape/radius/elevation:
  - 카드 반경은 기본 18pt, 내부 여백은 기본 18pt다.
  - 그림자·그라디언트 대신 배경 계층과 1px 이하의 미세한 테두리로 구분한다.
  - 촬영 가이드는 실제 검출 결과와 혼동되지 않게 점선/반투명으로 표시
- Motion:
  - 상태 전환은 짧은 페이드·이동을 사용한다.
  - MVP stepper는 2D 상태의 이전·다음·일시정지를 제공한다. 3D 회전은 후속 검증 전까지 기본 표현이 아니다.
  - 검사 시간에는 색상만으로 경고하지 않고 숫자·음성/햅틱을 병행한다.
- Imagery/iconography:
  - SF Symbols를 우선한다.
  - 큐브 도식은 면 방향과 회전 화살표를 일관된 규칙으로 그린다.
  - 장식용 3D 큐브보다 고정 방향 2D net과 필요한 면·조각 강조를 우선한다.

## Components

- Existing components to reuse:
  - `CoachCard`
  - `SectionHeading`
  - `ProgressLabel`
  - SwiftUI `NavigationStack`, `TabView`, `ProgressView`, `ContentUnavailableView`
- New/changed components:
  - `TrainerView`: 사용자-facing `복습` 화면. 시작 상태, 시각 인식 판별, 단계별 도움, 실제 큐브 수행과 수동 결과 비교
  - `MoveTimelineView`: 파싱된 move, 이전/다음 2D 상태, 첫/마지막 단계와 자동 재생
  - `CubeNetView`: 고정 면 배치, 색과 방향 문자·테두리, 변경 스티커 강조와 VoiceOver 면 설명
  - `NotationPrimerView`: 기준 면, 시계 방향 판정과 기본 표기 안내
  - `ScanPoseGuide`: 포즈 A/B, 검출 면, 조명·거리 피드백
  - `StickerCorrectionGrid`: 낮은 신뢰도 스티커만 빠르게 수정
  - `PracticeTimer`: 별도 push와 `ScrollView`가 없는 연습 루트. 인라인 내비게이션, 모드 선택, 스크램블, 축약 전개도·U/F 기준, 시간과 주요 조작을 한 viewport에 둔다. 소형 iPhone과 시스템 최대 콘텐츠 크기 요청에서도 필수 요소를 함께 유지하도록 이 계측기 내부의 Dynamic Type만 `Accessibility 1`까지 제한한다. 준비, 검사, 실행, 정지, +2/DNF를 같은 조작 위치에서 전환한다. 실행 중에는 persistent `UIButton`이 시간 지연 없이 새 touch sequence의 hit-test를 받고 첫 touch-down을 one-shot으로 정지하며, 내비게이션·탭 chrome을 숨긴다.
  - `ScrambleCard`: TNoodle 연습 스크램블 표기와 같은 스크램블을 완성 상태에 적용한 54-facelet 전개도를 한 단위로 표시한다. 흰색 U 위·초록 F 앞 검산 기준을 전개도 옆에 축약 표시하고, 둘 중 하나라도 만들지 못하면 타이머를 잠근다. 솔브 정지당 다음 presentation은 정확히 한 번 게시한다.
  - `SolveTrendChart`: 기간·세션별 기록 추세와 일관성을 접근 가능한 표와 함께 표시
  - 후속 `LearningTransferSummary`: 도움 수준, 지연 구간, 수동 일치 보고, 스캔 검증을 분리해 기록 통계와 연결
  - `SourceBadge`: 공식/학술/커뮤니티, 버전, 라이선스 상태
- Variants and states:
  - 내부 기록은 H0 시작 상태/H1 시각 인식 판별 단서/H2 잡는 방향·기준 면·표기 프라이머/H3 첫 move와 전후 상태/H4 첫 의미 청크와 stepper/H5 전체 stepper를 유지한다. 사용자 UI는 이 코드를 표시하지 않는다.
  - 습득 열람/인식 정답·교정/수동 일치·불일치·모름. 스캔 검증 일치·불일치는 후속 상태
  - 스캔 대기/정렬 중/촬영 가능/분석 중/수정 필요/유효/불가능 상태
  - 타이머 준비/검사/실행/정지/+2/DNF
  - 콘텐츠 초급/2-Look/Full CFOP/고급 분석
- Token/component ownership:
  - 색상·간격·반경은 `CubeCoachApp/DesignSystem`이 소유한다.
  - 큐브 상태·표기·스크램블·타이머 규칙은 UI가 아닌 Core 도메인이 소유한다.
  - 케이스 콘텐츠는 출처·버전을 포함한 별도 카탈로그가 소유한다.

## Accessibility

- Target standard: WCAG 2.2 AA에 준하는 iOS 접근성과 Apple 접근성 API. 이는 목표이며, 전체 앱의 Larger Text·VoiceOver 적합성을 실기기 검증 전에 완료된 지원으로 주장하지 않는다.
- Keyboard/focus behavior:
  - iPad 하드웨어 키보드와 VoiceOver 순서는 출시 전 검증할 지원 목표다.
  - 타이머 실행 중에는 화면 어디서든 첫 touch-down으로 정지할 수 있고, 전체 정지 표면에는 VoiceOver 접근성 액션을 구현한다.
  - 포커스는 힌트 공개 뒤 새 콘텐츠로 이동하되 사용자의 문맥을 잃지 않는다.
- Contrast/readability:
  - 큐브 색만으로 면과 조각을 구분하지 않는다. 현재 문자와 테두리를 함께 제공하며 패턴 옵션은 후속 검토한다.
  - 큰 글자에서 공식이 잘리지 않고 가로 스크롤 또는 줄바꿈 규칙을 제공한다.
  - 카메라 오버레이는 밝고 어두운 배경 모두에서 보이도록 이중선 또는 재질 배경을 쓴다.
- Screen-reader semantics:
  - 표기는 “알, 유, 알 프라임”처럼 설정 가능한 한국어 음성 설명을 제공한다.
  - 2D net에는 면 라벨, 케이스 이름, 강조 조각, 현재/기대 상태의 텍스트 대체를 제공한다.
  - 진행률과 타이머는 변화 빈도를 제한해 과도한 읽기를 막는다.
- Reduced motion and sensory considerations:
  - Reduce Motion 사용 시 큐브 회전을 단계 전환 또는 정적 프레임으로 바꾼다.
  - 음성·햅틱은 각각 끌 수 있고 시각 신호를 항상 병행한다.
  - 검사 시간 경고는 8초·12초 안내를 선택할 수 있게 한다.

## Responsive behavior

- Supported breakpoints/devices:
  - MVP: iPhone 세로, iOS 17 이상
  - 지원 목표: iPhone 가로와 iPad 분할 화면
  - 카메라 기능: 후면 카메라가 있는 실기기
- Layout adaptations:
  - 연습 루트는 세로 스크롤을 추가하지 않고 사용 가능한 높이에 따라 간격·전개도·타이머 크기를 축약해 한 viewport에 맞춘다. 시스템이 더 큰 콘텐츠 크기를 요청해도 계측기 descendants는 `Accessibility 1`을 상한으로 사용한다.
  - 좁은 폭에서는 한 열 카드와 하단 주요 버튼을 사용한다.
  - 넓은 폭에서는 케이스 도식과 힌트/공식을 2열로 배치할 수 있다.
  - 일반 읽기 화면은 큰 Dynamic Type에서 세로 확장을 목표로 한다. 고정형 연습 계측기의 상한을 전체 앱의 Larger Text 지원으로 일반화하지 않는다.
- Touch/hover differences:
  - 최소 44×44pt 터치 영역
  - 한 손 촬영 중 엄지로 닿는 하단에 촬영 버튼과 포즈 안내 배치
  - iPad 포인터 hover는 부가 피드백일 뿐 필수 정보를 담지 않는다.

## Interaction states

- Loading:
  - 카메라 준비, 모델 로드, 스크램블 생성, 상태 검증을 구분해 표시한다.
  - 400ms 이하의 짧은 작업에는 불필요한 전면 로더를 피한다.
- Empty:
  - 복습 없음: 자유 연습 또는 새 케이스 학습을 제안한다.
  - 기록 없음: 첫 스크램블과 타이머 시작 경로를 제공한다.
- Error:
  - 카메라 권한 거부: 설정 이동과 수동 입력 대안을 제공한다.
  - 인식 불확실: 전체 재촬영보다 낮은 신뢰도 칸만 수정하게 한다.
  - 불가능한 큐브 상태: 색 수 문제가 아닌 구체적 검증 실패를 설명하고 재촬영/수정을 제안한다.
- Success:
  - 현재 수동 비교에서는 “기대 상태와 같다고 기록했어요”처럼 보고 수준을 말한다. 후속 스캔 검증은 실제 iPhone 게이트와 기대 상태 비교를 통과한 경우에만 “유효 상태 스캔으로 일치를 확인했어요”라고 말한다.
  - 짧은 피드백 뒤 다음 복습 시점과 선택 가능한 다음 행동을 보여 준다.
- Disabled:
  - 비활성 이유를 라벨과 접근성 힌트로 설명한다.
  - 신뢰도가 낮다는 이유만으로 수동 수정 경로까지 막지 않는다.
- Offline/slow network:
  - 핵심 학습, 타이머, 기록, 온디바이스 인식은 오프라인 우선이다.
  - 외부 영상·커뮤니티 링크는 연결 필요 상태를 명시하고 핵심 학습을 막지 않는다.

## Content voice

- Tone: 짧고 구체적이며 판단하지 않는 `해요체` 코치 말투. 제목·라벨은 명사형, 설명·피드백은 해요체를 기본으로 한다.
- Terminology:
  - 처음에는 “윗면(U)”처럼 한국어와 표기를 함께 쓰고 익숙해지면 표기를 우선한다.
  - `공식`은 외울 동작열, `해법`은 단계 체계, `케이스` 또는 `패턴`은 인식할 시각 상태로 구분한다.
  - `솔브`는 측정한 한 번의 시도, `솔빙`은 큐브를 푸는 활동 전체에만 사용한다.
  - 학습 화면을 본 사실이나 수동 비교 시도는 `복습 기록`으로 부른다. 자동 확인 근거가 없으면 `완료`, `숙달`, `검증`으로 부르지 않는다.
  - `최단`은 실제 최적성이 증명된 경우에만 쓰고, 그 외에는 `준최단` 또는 `짧은 해`라고 한다.
  - 사용자 선택 이름은 `자유 연습`, `15초 인스펙션`으로 고정한다. WCA는 규칙 출처 설명에만 쓰고 공식 대회 기능과 구분한다.
  - 현재 수동 결과는 `일치 보고`라고 한다. 후속 `검증됨`은 유효 상태 검사와 기대 상태 비교를 통과한 스캔에만 사용한다.
  - 한국어 면 이름은 `윗면(U)`, `앞면(F)`처럼 WCA 표기와 병기한다. `알 프라임` 같은 발음은 사용자 테스트 전까지 표준이라고 부르지 않는다.
- Microcopy rules:
  - 버튼은 `복습 시작`, `인스펙션 시작`, `돌리기 완료`처럼 결과가 분명한 동사형을 쓴다.
  - 한 문장에는 한 행동만 요청하고, 내부 구현 용어를 행동 설명보다 먼저 두지 않는다.
  - “틀렸어요” 대신 “이 케이스는 다시 볼 시점이에요.”
  - “정답 보기”보다 “방향 보기”, “기대 상태 보기”, “부분/전체 stepper 열기”처럼 공개 수준을 말한다.
  - 후속 “스캔 검증 완료”는 54칸 상태 유효성 검사와 기대 상태 비교를 통과했을 때만 사용한다. 현재 단계 진단에는 쓰지 않는다.
  - 인식 신뢰도는 백분율만 제시하지 않고 “3칸 확인 필요”처럼 행동으로 번역한다.
  - 사용자 화면에는 `Trainer`, `H0`–`H5`, `guidedAcquisition`, `externallyPrepared`를 표시하지 않는다. 각각 `복습`, 구체적인 도움 이름, `시작 상태 만드는 법을 봄`, `이미 같은 상태로 준비함`처럼 행동과 결과를 말한다.

## Implementation constraints

- Framework/styling system:
  - Swift 6, SwiftUI, iOS 17 이상
  - AVFoundation 카메라, Vision 사각형/윤곽 검출, 필요 시 Core ML 온디바이스 모델
  - 외부 UI 프레임워크를 추가하지 않는다.
- Design-token constraints:
  - 시스템 색·Dynamic Type·SF Symbols를 우선한다.
  - 큐브의 여섯 색은 사용자 색 구성과 촬영 보정에 따라 달라질 수 있으므로 고정 RGB를 상태의 진실로 사용하지 않는다.
- Performance constraints:
  - 카메라 세션과 Vision/Core ML 처리는 메인 스레드 밖에서 수행한다.
  - 라이브 프레임은 제한 주기로 분석하고 촬영 순간에 고해상도 정밀 분석을 수행한다.
  - 타이머 표시는 프레임 갱신과 시간 측정 원천을 분리하고 단조 시계를 사용한다.
  - 번들 TNoodle 출력은 유한 연습 카탈로그라고 표시하며 앱 자체가 무제한 random-state 생성기인 것처럼 표현하지 않는다.
- Compatibility constraints:
  - 시뮬레이터에는 카메라 대체 흐름이 필요하다.
  - 스티커형·스티커리스 큐브, 로고 센터, 비표준 색상 배열은 수동 확인 경로가 필요하다.
  - iOS 카메라 권한 설명 문자열과 App Store 개인정보 표시는 출시 전에 검수한다.
- Test/screenshot expectations:
  - 모든 주요 화면에 라이트/다크, 기본/접근성 글자 크기 스냅샷을 둔다.
  - 타이머 경계값 8/12/15/17초와 +2/DNF를 단위 테스트한다.
  - 표기 파싱, 스크램블 재현, 큐브 상태 불변식을 단위·속성 테스트한다.
  - 내장 학습 항목의 시작 상태에서 각 move를 적용한 모든 stepper 중간·최종 상태를 검증한다.
  - 현재 수동 비교 결과에 `검증됨`이 나타나지 않는지 UI 테스트한다. validated scan UI는 사후 비교 통합 뒤 별도로 테스트한다.
  - 다양한 기기/조명/큐브 재질의 고정 데이터셋으로 포즈별 검출·색상·완성 상태 정확도를 회귀 검증한다.

## Open questions

- [ ] 초급 해법의 기준 공식과 한국어 명칭을 어떤 권리·검수 절차로 확정할지 / 콘텐츠 책임자 / 잘못된 공식과 저작권 위험
- [ ] MVP에 2-Look OLL·PLL 중 어디까지 포함할지 / 제품 책임자 / 일정과 학습 완결성
- [ ] 두 포즈 사이 연속 영상을 짧게 추적할지, 정지 사진 두 장만 저장 없이 처리할지 / 비전 책임자 / 방향 정합 정확도와 개인정보
- [ ] 비표준 색상 배열을 MVP에서 지원할지 / 제품·비전 책임자 / 수동 입력 복잡도
- [ ] `최단 해법`을 사후 비교 기능으로 제공할지, MVP에서 제외할지 / 제품 책임자 / 대전제 훼손과 계산 비용
- [ ] 32,768개 번들 카탈로그 이후 무제한 random-state 서비스가 실제로 필요한 시점과 구현을 확정할지 / 코어 책임자 / 공정성·성능·라이선스
- [ ] 케이스별 대체 공식과 손잡이·핑거트릭 개인화를 언제 도입할지 / 학습 책임자 / 콘텐츠 규모
- [ ] 접근성용 큐브 패턴·기호 체계를 어떤 사용자 테스트로 검증할지 / 디자인 책임자 / 색각 사용자 사용성
- [ ] 학습 스케줄러를 단순 규칙에서 어떤 근거와 데이터로 조정할지 / 학습 책임자 / 과도한 개인화·효과 주장
- [ ] 2D net·stepper가 실제 큐브 방향 오류와 동작 오류를 줄이는지 / 학습·디자인 책임자 / 큐브 전용 효과 근거 부재
- [ ] 수동 일치 보고와 스캔 검증의 차이를 사용자가 이해하는지 / 리서치 책임자 / 과도한 `검증` 인식
